import Head from "next/head";
import Navbar from "../components/Navbar";
import Header from "../components/Header";

import { Layout } from "antd";
const { Footer } = Layout;

export const metadata = {
  title: "E-commerce",
  description: "Generated by create next app",
};

export default function RootLayout({
  children,
  props,
}: {
  children: React.ReactNode;
  props: string;
}) {
  return (
    <>
      <Head>
        <meta name="E-commerce" />
      </Head>

      <main>
        <Navbar />
        <div className="flex flex-col py-20 gap-[3rem] px-[15.5%] bg-bodyBg min-h-screen responsive:justify-center responsive:px-[4%]">
          <Header breadcrumb={props} />
          {children}
        </div>
        <Footer className="flex flex-wrap text-[1.5vh] justify-center">
          Copyright &copy; 2023 Criado por Lucas Peixoto
        </Footer>
      </main>
    </>
  );
}
